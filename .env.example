# Copy to .env and fill in. .env is gitignored.
# This is the only environment variable the code reads.
#
# Everything else (image model, storage paths, upload limits, size presets) lives in
# backend/config.py as constants on purpose: the engine choice is a Product.md decision
# and must not be swappable through the environment (NonGoals.md #2).
OPENAI_API_KEY=
