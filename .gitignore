.env
data/
storage/
*.db
*.db-wal
*.db-shm

scripts/risk_gate_output.png

# AC-5 material: catalogue exports in, generated results out. Both are large image sets,
# and the verdicts that matter live in results.csv, which you can copy out by hand.
ac5-source/
ac5-results/

.venv/
__pycache__/
*.pyc
.pytest_cache/

# catalog/products.json IS committed on purpose: it is the deliverable of Product.md 8.1,
# and the 200 MB PDF it comes from is not in the repo, so this file is the data.
