# วิธีติดตั้ง (สำหรับผู้รับมอบ)

โปรแกรมนี้รันบนเครื่องตัวเอง ไม่ใช่เว็บออนไลน์ — เปิดใช้ได้เฉพาะเครื่องที่ติดตั้งไว้

---

## ต้องมีอะไรบ้าง

1. **Windows** (ทดสอบบน Windows 11)
2. **ไฟล์ที่ได้รับมอบ** — โฟลเดอร์โปรเจกต์ทั้งอัน วางไว้ที่ไหนก็ได้ที่จำง่าย เช่น `D:\TreeDecorator\`
3. **OpenAI API key** — ขอที่ <https://platform.openai.com/api-keys>
   บัญชีต้องผูกบัตร/เติมเงินไว้แล้ว เพราะโมเดลสร้างภาพไม่มีโควตาฟรี

**ไม่ต้องลง Python** — ตัวติดตั้ง (uv) จะโหลดมาให้เองอัตโนมัติ

---

## ขั้นตอน (ทำครั้งเดียว)

### 1. ติดตั้ง uv

เปิด **PowerShell** แล้ววางคำสั่งนี้ กด Enter:

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

รอจนขึ้นว่าเสร็จ แล้ว **ปิด PowerShell ทิ้ง เปิดใหม่** (จำเป็น ไม่งั้นเครื่องยังไม่รู้จักคำสั่ง `uv`)

เช็คว่าติดตั้งผ่านไหม พิมพ์:

```powershell
uv --version
```

ถ้าขึ้นเลขเวอร์ชัน = ผ่าน

### 2. ดับเบิลคลิก `setup.bat`

อยู่ในโฟลเดอร์โปรเจกต์ ชื่อ **setup.bat** — ดับเบิลคลิกได้เลย

มันจะทำให้อัตโนมัติ:
- โหลด Python + ไลบรารีที่ต้องใช้ (ครั้งแรกใช้เวลาสักพัก มีโหลดของเยอะ)
- แตกไฟล์รูปสินค้า `catalog-images.zip` ให้ ถ้ายังไม่ได้แตก
- ถาม API key
- เปิดเว็บให้เอง

### 3. ใส่ API key ตอนที่มันถาม

พอขึ้นบรรทัด `OPENAI_API_KEY=` ให้วาง key แล้วกด Enter
(ใส่ครั้งเดียว รอบหน้าไม่ถามอีก เก็บไว้ในไฟล์ `.env` ในเครื่อง ไม่ได้ส่งไปไหน)

### 4. ตอบคำถาม 1 ข้อ

```
Run the engine check now? It makes one real billed generation. [y/N]
```

**แปลว่า**: จะลองสร้างภาพจริง 1 รูปเพื่อเช็คว่า API key กับ billing ใช้ได้จริงไหม — **เสียเงินจริง 1 ครั้ง**

- **ครั้งแรกที่ติดตั้ง แนะนำพิมพ์ `y`** — เสียนิดเดียว แต่รู้ทันทีว่าใช้ได้จริงหรือ key มีปัญหา ดีกว่ามารู้ตอนใช้งานจริง
- ถ้าไม่อยากลอง พิมพ์ `n` หรือกด Enter เฉย ๆ ก็ข้ามได้

### 5. เสร็จ

Browser จะเปิดขึ้นมาเองที่ <http://localhost:8000>

**หน้าต่างดำ ๆ (คอนโซล) ห้ามปิด** — ปิดเมื่อไหร่โปรแกรมดับทันที ย่อเก็บไว้ได้

---

## ครั้งต่อ ๆ ไป

ดับเบิลคลิก **setup.bat** อันเดิม — มันข้ามขั้นที่ทำไปแล้วให้เอง เปิดเว็บให้ภายในไม่กี่วินาที

จะเลิกใช้ก็ปิดหน้าต่างดำ ๆ นั้น

---

## เช็คว่าติดตั้งครบไหม

เข้า <http://localhost:8000/settings> ต้องเห็น:

| หัวข้อ | ต้องขึ้นว่า |
|---|---|
| OpenAI API key | ตั้ง key ไว้แล้ว |
| ขนาดสินค้า | โหลดแล้ว |
| ค้นของจากรูป | พร้อมใช้ |

ถ้าครบทั้ง 3 = ใช้งานได้เต็มระบบ

---

## ถ้ามีปัญหา

**พิมพ์ `uv` แล้วบอกไม่รู้จักคำสั่ง**
→ ยังไม่ได้ปิด-เปิด PowerShell ใหม่หลังติดตั้ง uv ปิดแล้วเปิดใหม่

**setup.bat เด้งปิดทันที**
→ คลิกขวาที่ setup.bat เลือก Edit ดูว่าไฟล์ครบไหม หรือเปิด PowerShell แล้วพิมพ์
`cd "โฟลเดอร์โปรเจกต์"` แล้ว `.\setup.bat` จะเห็นข้อความ error ค้างไว้อ่านได้

**เว็บเปิดไม่ขึ้น / หน้าขาว**
→ ดูหน้าต่างดำ ๆ ว่าขึ้น `Uvicorn running on http://127.0.0.1:8000` หรือยัง ถ้ายังให้รออีกหน่อย

**พอร์ต 8000 ถูกใช้อยู่**
→ มีโปรแกรมนี้เปิดค้างอยู่แล้วอีกหน้าต่าง ปิดอันเก่าก่อน

**"ค้นของจากรูป" ขึ้นว่ายังไม่พร้อม**
→ ข้อมูลค้นหาไม่ได้มาด้วย กดปุ่ม **sync ดัชนีค้นหา** ในหน้าตั้งค่า
(เสียเงินตามจำนวนสินค้า ~1,250 ชิ้น กดค้างไว้ได้ ถ้าหลุดกลางทางกดใหม่ได้ ไม่จ่ายซ้ำของที่ทำไปแล้ว)

**รูปสินค้าใน catalogue ไม่ขึ้น**
→ `catalog-images.zip` ไม่ได้อยู่ในโฟลเดอร์ตอนรัน setup.bat
เอาไฟล์นั้นไปวางไว้ในโฟลเดอร์โปรเจกต์ แล้วรัน setup.bat ใหม่

---

## เรื่องค่าใช้จ่าย

โปรแกรมไม่เก็บเงิน — จ่ายให้ OpenAI ตรง ๆ ตามที่ใช้

**ขั้นที่เสียเงิน** มีอย่างเดียวคือกดปุ่ม **"สร้างภาพ"** (ถามยืนยันทุกครั้งก่อนเสมอ)

ขั้นอื่นฟรีหมด — อัปโหลด, ตัดพื้นหลัง, เลือกจาก catalogue, ดูประวัติ

ดูยอดใช้จริงได้ที่ <https://platform.openai.com/usage>
